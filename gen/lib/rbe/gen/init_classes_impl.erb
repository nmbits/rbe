
#include "rbe.hpp"
% target.each do |c|
#include "<%= class_binding_name c.name %>.hpp"
% end

namespace rbe
{
	void
	init_classes(VALUE mod)
	{
% target.each do |c|
		B::<%= class_binding_name c.name %>::rbe__init_class(mod);
% end
	}
}
